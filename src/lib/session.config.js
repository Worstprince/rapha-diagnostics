// lib/session.config.js
// Shared between the login/logout routes and the server-side session reader,
// so the cookie name only lives in one place.

export const SESSION_COOKIE_NAME = "rd_session";
